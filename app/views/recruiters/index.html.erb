<!-- hide show on click -->
<div ng-if="newPosting" class="newpostScreen">
	<div class="newpostInner">
	<!-- post job -->
	<form ng-submit="makeNewPosting(title, description, postingTagArray)">
		<h2>Job Title</h2>
		<input type="text" id="inputTitle" ng-model="title" name="text" />
		<h2>Description</h2>
		<textarea type="text" id="inputDesc" ng-model="description" name="text" /></textarea>

		<!-- tags for job postings -->
		<div class="inline-button" ng-repeat="tag in tags">
			<!-- each tag item/button -->
			<div class="button" ng-click="postingAddTag(tag.name, checkPostingArray(tag.name))" 
				ng-class="{selected: !checkPostingArray(tag.name)}">
				{{tag.name}}
			</div>
		</div>
		<input id="submitPost" class="btn" type="submit" id="submit" value="Submit" />
	</form>
	</div>
</div>

<!-- Recruiter's Postings -->
<div ng-if="showPosting" id="postingScreen">
	<div id="postingsContainer">
		<h2>My Postings</h2>
		<div id="allPostings">
			<div ng-repeat="posting in postings">	
				<h2>{{posting.title}}</h2>
				<p>{{posting.description}}</p>
				<div class="likedislike">
					<div class="likestat"><%=image_tag 'like.svg', :class=>"like"%> {{posting.like}}</div>
					<div class="dislikestat"><%=image_tag 'dislike.svg', :class=>"dislike"%> {{posting.dislike}}</div>
					<div>{{posting.created_at}}</div>
				</div>
			</div>
		</div>
	</div>
</div>

<!-- Search for Applicants -->
<!-- repeat tags -->
<div ng-if="searchDiv" id="searchScreen">
	<div id="searchContainer">

		<div class="col-md-4">
			<!-- search button-->
			<div class="btn searchButtons" ng-click="search(tagArray)">Search</div>

			<div class="btn searchButtons" ng-click="reset()">Reset</div>
			<div class="tags-container">
				<div class="inline-button scrolly" ng-repeat="tag in tags">
					<!-- each tag item/button -->
					<div class="button" ng-click="addTag(tag.name, checkArray(tag.name))" 
						ng-class="{selected: !checkArray(tag.name)}">
						{{tag.name}}
					</div>
				</div>
			</div>
		</div>

		
		<div class="col-md-8">
			<!-- search results -->
			<h2 ng-if="applicants!=null">Matching Applicants Results</h2>

			<!-- carousel -->
			<div id="searchResults">
			<!-- display if zero results -->
				<div ng-if="applicants.length==0">No Results</div>

				<div ng-repeat="applicant in applicants" >
					<div ng-init="carousel[$index]=false; carousel[0]=true">
						<div ng-if="carousel[$index]">
							<div ng-click="backwards($index)">Back</div>
							<div>{{$index+1}} of {{applicants.length}}</div>
							<div>Name: {{searchnames[$index]}}</div>
							<div>Email: {{searchemails[$index]}}</div>
							<div>Link: {{applicant["link"]}}</div>
							<div ng-click="forwards($index)">Fowards</div>
						</div>
					</div>
				</div>
			</div>
		</div>
	</div>
</div>



<!-- Notificiations -->
<div ng-if="showNotifications" id="nDiv">
	<div id="notificationsContainer">
		<h2>Notifications</h2>
		<div id="notifications">
			<div ng-repeat="n in notifications">
				<div ng-init="ncarousel[$index]=false; ncarousel[0]=true">
					<div ng-if="ncarousel[$index]">
						<div ng-click="nbackwards($index)">Back</div>
						<div ng-click="removeNotification(n.id, showP[n.id])" class="button">Remove Notification</div>
						
						<div>{{$index+1}} of {{notifications.length}}</div>

						<div>{{names[$index]}} liked your post at {{n["created_at"]}}</div>

						<div ng-init="showP[n.id]=false" ng-click=" showP[n.id]=!showP[n.id]" class="button">See Profile</div>

						<div ng-if="showP[n.id]">
							<div>Name: {{names[$index]}}</div>
							<div>Email: {{emails[$index]}}</div>
							<div>Links: {{profiles[$index]["link"]}}</div>
						</div>
						<div ng-click="nforwards($index)">Fowards</div>
					</div>
				</div>
			</div>
		</div>
	</div>
</div>
